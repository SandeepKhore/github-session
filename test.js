// this is test file for github session

//  master 

// what to keep in mind before starting any work
// current = master

if(currentBranch == 'master') {
    // create new branch for the changes
    // commands to create new branch
    // 1. git branch [branch_name] # if using this command we have to first switch to the new branch (git checkout branch_name)
    // 2. git checkout -b [branch_name] 
} else {
    // if the branch is empty start working on
    // if not (merge changes in master)
    // two ways to merge current branch in master
    // 1. git merge [branch-name]
    // 
}


